import 'package:flutter/material.dart';

class OpacityLevels {
  static const Opacity semiOpaque = Opacity(opacity: 0.80);
  static const Opacity intense = Opacity(opacity: 0.64);
  static const Opacity medium = Opacity(opacity: 0.32);
  static const Opacity light = Opacity(opacity: 0.16);
  static const Opacity semiTransparent = Opacity(opacity: 0.08);
}
